using UnityEngine;
using System.Collections;

public class HeadSetReferenceSetter : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        //this.transform.position = GameObject.Find("Camera (eye)").transform.position;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
